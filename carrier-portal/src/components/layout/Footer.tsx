export function Footer() {
  return (
    <footer className="mt-16 border-t border-border bg-white">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-text-muted md:flex-row md:items-center md:justify-between">
        <p>HopDrop · Intercity package relay by verified travelers</p>
        <p>OTP secured · Safety deposit protected · Razorpay enabled</p>
      </div>
    </footer>
  );
}
